const nome = 'Erick';
const idade = 17;

// Quando o nome da propriedade for o mesmo da váriavel eu passo passar só um deles
const usuario = {
    nome, 
    idade,
    empresa: 'ErickCorps'
}

console.log(usuario);