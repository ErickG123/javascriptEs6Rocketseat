"use strict";

var nome = 'Erick';
var idade = 17; // Quando o nome da propriedade for o mesmo da váriavel eu passo passar só um deles

var usuario = {
  nome: nome,
  idade: idade,
  empresa: 'ErickCorps'
};
console.log(usuario);
